# Exakte Versionen, gleiche Begruendung wie in src/requirements.txt (Wunsch
# #135) - hier nachgezogen beim Sicherheitsaudit am 11.08.2026, wo auffiel,
# dass nur die App gepinnt war und der util-Container weiterhin mit ">="
# baute. Ein "docker compose up --build util" konnte damit jederzeit neue
# Versionen einziehen, ungefragt und unbemerkt.
#
# Wunsch #280 (17.09.2026): Der einzige Nutzer von `requests` war
# cert_watcher.py; der spricht die Caddy-Admin-API jetzt per http.client
# ueber einen Unix-Socket an. Damit braucht util KEIN Fremdpaket mehr -
# scheduler, db_snapshot, backup und cert_watcher kommen mit der
# Standardbibliothek aus. Die Datei bleibt (leer) bestehen, weil das
# Dockerfile sie kopiert und installiert; ein kuenftiges Paket gehoert
# exakt gepinnt hierher.
