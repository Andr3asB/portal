# Wunsch #135: exakte Versionen statt ">=". Mit ">=" zog jedes
# "docker compose up --build" potenziell neue Versionen - ein kompromittiertes
# Paket-Release waere ungefragt in der Produktion gelandet, und zwei Builds
# vom selben Stand waren nicht zwangslaeufig identisch (schlecht beim
# Fehlersuchen). Stand entspricht dem, was am 2026-08-05 im Container lief.
# Aktualisieren heisst ab jetzt: Version hier bewusst hochsetzen, neu bauen,
# testen, im journal.md vermerken.
Flask==3.1.3
gunicorn==26.0.0
pywebpush==2.3.0
segno==1.6.6
tzdata==2026.3
dicebear-core==10.3.2
dicebear-styles==10.2.0
# Wunsch #143: Barcode vom Foto lesen. Serverseitig statt im Browser,
# weil die Browser-API `BarcodeDetector` weder auf iOS noch in Chrome
# unter Windows existiert (nachgemessen) - sie haette ausgerechnet auf
# den Geraeten gefehlt, mit denen eingekauft wird. Reine Wheels, keine
# System-Pakete noetig; zusammen rund 9 MB im Abbild.
zxing-cpp==3.1.1
pillow==12.3.0

# ---------------------------------------------------------------------------
# Transitive Abhaengigkeiten - gepinnt seit dem Sicherheitsaudit 11.08.2026.
#
# Warum das hier dazugehoert: Bis dahin waren nur die neun Pakete oben
# festgenagelt. Alles, was sie ihrerseits nachziehen - darunter Werkzeug,
# Jinja2, cryptography, urllib3 und requests -, kam ungepinnt mit. Damit war
# genau die Luecke offen, die Wunsch #135 schliessen wollte: Ein
# "docker compose up --build" konnte weiterhin neue Versionen einziehen, und
# zwei Builds vom selben Quellstand waren nicht zwangslaeufig identisch. Dass
# Flask festgenagelt ist, nuetzt wenig, wenn Werkzeug (wo die Request-
# Verarbeitung tatsaechlich passiert) frei mitwandert.
#
# Die Werte sind KEINE Wunschliste, sondern der exakte Stand, der am
# 11.08.2026 im laufenden portal-Container installiert war (`pip freeze`).
# Ein Rebuild aendert damit funktional nichts - er wird nur reproduzierbar.
#
# Geprueft mit `pip-audit` gegen denselben Stand: keine bekannten
# Schwachstellen (siehe requirements-dev.txt fuer den Befehl).
#
# Beim Hochsetzen eines Pakets oben: dieser Block ist danach womoeglich
# veraltet. Neu erzeugen mit
#   docker exec portal pip freeze
# und die Zeilen, die nicht schon oben stehen, hier ersetzen.
# ---------------------------------------------------------------------------
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
attrs==26.1.0
blinker==1.9.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
cryptography==50.0.0
dicebear-schema==1.3.0
frozenlist==1.8.0
http_ece==1.2.1
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
multidict==6.7.1
packaging==26.3
propcache==0.5.2
py-vapid==1.9.4
pycparser==3.0
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
typing_extensions==4.16.0
urllib3==2.7.0
Werkzeug==3.1.8
yarl==1.24.5
